<form method="post" action="{{ path('app_avistamientos_delete', {'id': avistamiento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avistamiento.id) }}">
    <button class="btn">Delete</button>
</form>
